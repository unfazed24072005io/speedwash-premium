name: speedwash_premium
description: SpeedWash Premium Car Wash App
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # ✅ ADD THESE - Your main.dart NEEDS them:
  firebase_core: ^2.32.0
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.17.5
  firebase_messaging: ^14.7.10
  razorpay_flutter: ^1.4.0
  geolocator: ^10.1.1
  permission_handler: ^10.4.5
  url_launcher: ^6.1.0
  share_plus: ^7.2.2
  vibration: ^1.9.0
  intl: ^0.18.1
  google_fonts: ^4.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
